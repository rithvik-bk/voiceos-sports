{
  "name": "voiceos-sports-integration",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "NFL & NBA live sports companion inside VoiceOS — ESPN keyless (zero setup). Self-contained MCP server (Node/Bun built-ins only), native notch cards.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "bun build server.ts --target=node --format=esm --outfile=server.mjs",
    "start": "node server.mjs"
  },
  "main": "server.mjs"
}
